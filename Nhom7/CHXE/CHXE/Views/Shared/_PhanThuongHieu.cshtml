@model IEnumerable<CHXE.Models.ThuongHieu>
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="far fa-registered"></i> Thương Hiệu
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        @foreach (var item in Model)
        {
        
            <a class="dropdown-item" href="@Url.Action("XeTheoThuongHieu","Home",new {  id = item.ID})"><i class="fab fa-pied-piper"></i> @item.TenThuongHieu</a>
        }
     </div>
</li>